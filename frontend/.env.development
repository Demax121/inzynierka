# Plik: frontend/.env.development
# Uwaga: Vite wczytuje zmienne tylko z prefiksem VITE_.
# Podaj pełne adresy z protokołem, aby fetch używał poprawnych URL-i w przeglądarce.

# Backend (Caddy serwujący pliki PHP)
# Jeśli używasz docker-compose i mapujesz port np. 8884 -> 80, to z przeglądarki korzystaj z localhost:8884
VITE_BACKEND_URL_PREFIX=http://localhost:8884/

# CDN (Caddy z assetami)
VITE_CDN_URL_PREFIX=http://localhost:8885/

# WebSocket (serwer Bun)
VITE_WS_URL_PREFIX=ws://localhost:8886

# WLED (urządzenie w sieci lokalnej) – bez końcowego slasha, bo endpointy zaczynają się od '/'
VITE_WLED_URL_PREFIX=http://192.168.1.25

# Opcjonalne endpointy WLED – pozostaw, jeśli masz inne ścieżki niż domyślne
# VITE_WLED_PRESETS_ENDPOINT=/presets
# VITE_WLED_STATE_ENDPOINT=/json/state
